Write react application with 5 components

Criteria
    - At least one component should have a fetch in it
    - At least one component should have imported functions
    - At least one component should have a child component nested inside the jsx
    - All the 5 components should be 100% covered
